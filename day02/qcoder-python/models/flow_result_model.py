"""
流程结果模型
Generated by scaffold script
"""
from sqlalchemy import Column, BigInteger, String, Text, SmallInteger, Integer, DateTime
from core.db import Base


class FlowResult(Base):
    """流程结果表模型"""
    
    __tablename__ = 'flow_result'
    
    flow_result_id = Column(BigInteger, primary_key=True, autoincrement=True, comment="流程结果id")
    flow_id = Column(BigInteger, comment="流程id")
    result_id = Column(BigInteger, comment="结果id")
    expected_result = Column(Text, comment="预期结果")
    status = Column(SmallInteger, comment="状态")
    remark = Column(Text, comment="备注")
    creator = Column(String(50), comment="创建人")
    create_time = Column(DateTime, comment="创建时间")
    update_time = Column(DateTime, comment="更新时间")

    def __repr__(self):
        return f"<FlowResult(flow_result_id={self.flow_result_id})>"
