"""
流程结果Schema
Generated by scaffold script
"""
from pydantic import BaseModel, ConfigDict, Field, field_serializer
from pydantic.alias_generators import to_camel
from typing import Optional
from datetime import datetime


class FlowResultBase(BaseModel):
    """流程结果基础Schema"""
    model_config = ConfigDict(
        alias_generator=to_camel,
        populate_by_name=True,
        from_attributes=True
    )
    
    flow_id: Optional[int] = Field(None, description="流程id")
    result_id: Optional[int] = Field(None, description="结果id")
    expected_result: Optional[str] = Field(None, description="预期结果")
    status: Optional[int] = Field(None, description="状态")
    remark: Optional[str] = Field(None, description="备注")


class FlowResultCreate(FlowResultBase):
    """流程结果创建Schema"""
    pass


class FlowResultUpdate(FlowResultBase):
    """流程结果更新Schema"""
    pass


class FlowResultInfo(FlowResultBase):
    """流程结果响应Schema"""
    flow_result_id: Optional[int] = Field(None, description="流程结果id")
    creator: Optional[str] = Field(None, description="创建人")
    create_time: Optional[datetime] = Field(None, description="创建时间")
    update_time: Optional[datetime] = Field(None, description="更新时间")

    @field_serializer('create_time', 'update_time')
    def serialize_datetime(self, dt: Optional[datetime], _info) -> Optional[str]:
        """时间字段自动转换为字符串格式"""
        if dt is None:
            return None
        return dt.strftime('%Y-%m-%d %H:%M:%S')
