"""
流程结果CRUD操作
Generated by scaffold script
"""
from sqlalchemy.orm import Session
from sqlalchemy import and_, func
from models.flow_result_model import FlowResult
from typing import List, Optional, Tuple


async def get_flow_result_by_id(db: Session, result_id: int) -> Optional[FlowResult]:
    """根据ID获取流程结果"""
    return db.query(FlowResult).filter(FlowResult.result_id == result_id).first()


async def get_flow_result_list(
    db: Session, 
    page_num: int = 1, 
    page_size: int = 10
) -> Tuple[List[FlowResult], int]:
    """获取流程结果分页列表
    
    Returns:
        Tuple[List[FlowResult], int]: (数据列表, 总记录数)
    """
    # 计算偏移量
    offset = (page_num - 1) * page_size
    
    # 查询总数
    total = db.query(func.count(FlowResult.result_id)).scalar()
    
    # 查询分页数据
    items = db.query(FlowResult).offset(offset).limit(page_size).all()
    
    return items, total


async def create_flow_result(db: Session, data: dict) -> FlowResult:
    """创建流程结果"""
    db_obj = FlowResult(**data)
    db.add(db_obj)
    db.commit()
    db.refresh(db_obj)
    return db_obj


async def update_flow_result(db: Session, result_id: int, data: dict) -> Optional[FlowResult]:
    """更新流程结果"""
    db_obj = db.query(FlowResult).filter(FlowResult.result_id == result_id).first()
    if db_obj:
        for key, value in data.items():
            setattr(db_obj, key, value)
        db.commit()
        db.refresh(db_obj)
    return db_obj


async def delete_flow_result(db: Session, result_id: int) -> Optional[FlowResult]:
    """删除流程结果"""
    db_obj = db.query(FlowResult).filter(FlowResult.result_id == result_id).first()
    if db_obj:
        db.delete(db_obj)
        db.commit()
    return db_obj
